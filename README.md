# Expense Tracker

A terminal app I built to track daily expenses without overcomplicating it.

## What it does

- Add and delete expenses
- Organise by category
- View monthly summaries through the summary option
- Saves everything to a JSON file so your data doesn't disappear

## How to run

```bash
python expense_tracker.py
